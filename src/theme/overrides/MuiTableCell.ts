import { colors } from '@material-ui/core';

export default {
  root: {
    fontSize: 14,
  },
  body: {
    borderBottom: `1px solid ${colors.grey[200]}`,
  },
  head: {
    paddingTop: 10,
    paddingBottom: 10,
    borderBottom: `1px solid ${colors.grey[200]}`,
    color: colors.blueGrey[500],
  }
};
