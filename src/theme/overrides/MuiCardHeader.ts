// import { colors } from '@material-ui/core';

import { colors } from "@material-ui/core";

export default {
  root: {
    borderBottom: `1px solid ${colors.grey[200]}`,
  },
  action: {
    marginRight: 0,
    marginTop: 0,
  }
};
